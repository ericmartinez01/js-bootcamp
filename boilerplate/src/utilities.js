// Named export -- you can have as many as you need
// Default export -- at most you get 1 default function 

const add = (a, b) => a + b

const name = 'Andrew'

const square = (x) => x * x

export { add, name, square }

